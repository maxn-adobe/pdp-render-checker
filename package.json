{
  "name": "pdp-render-checker",
  "version": "1.0.0",
  "private": true,
  "description": "Headless render check for Zazzle-backed AEM Edge Delivery PDPs",
  "scripts": {
    "check": "node check.mjs"
  },
  "devDependencies": {
    "playwright": "^1.58.0"
  }
}
